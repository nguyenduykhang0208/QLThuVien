Hướng dẫn chạy chương trình:
B1: sử dụng sql server attach database(file .mdf)
B2:Sửa web config với địa chỉ local server
B3:Nếu lỗi migration:
chạy lệnh:
update-database (sử dụng package console manage)
B4:Run code